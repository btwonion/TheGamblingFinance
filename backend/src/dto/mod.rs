//! Request / response DTOs for the HTTP layer.
//!
//! Phase 0 is empty. Phase 1 adds one module per route group
//! (e.g. `nights.rs`, `trades.rs`) mirroring the schemas in
//! `docs/contracts/openapi.json`.
